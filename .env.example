# Required for live transcription + MoM generation.
# If unset, the app runs in demo mode: transcript→MoM heuristics work; audio STT needs a key.
GEMINI_API_KEY=

# Optional — prefer OpenAI when set
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
AI_PROVIDER=gemini

# Server
PORT=3000
HOST=0.0.0.0

# Required for transcription + MoM generation
GEMINI_API_KEY=

# Optional — prefer OpenAI when set
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
AI_PROVIDER=gemini

# Server
PORT=3000
HOST=0.0.0.0

# Auth users persist under data/auth/ (gitignored)
# Signup/signin: POST /api/v1/auth/signup | /api/v1/auth/signin
